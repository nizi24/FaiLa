.article-likes
  %span.to-like Likes
  /* logged_inがないと、ログインしていないときにcurrent_userがnilになりエラー */
  - if logged_in? && current_user.already_liked?(@article.id)
    = button_to article_like_path(@article), method: :delete, class: 'like-icon' do
      %i.fas.fa-heart.fa-2x
  - else
    = button_to article_likes_path(@article), class: 'like-icon' do
      %i.far.fa-heart.fa-2x

- if @received_replies

  .received-replies-hide.btn-light
    %span.hide #{ @received_replies.length }件の返信を表示する

  .received-replies
    - @received_replies.each do |micropost|
      %hr
      = render 'microposts/micropost_detail', micropost: micropost

      - if micropost.received_replies
        .replies
          = render 'replies/replies', micropost: micropost
